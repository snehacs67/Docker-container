repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9  # Use the latest stable Ruff version
    hooks:
      - id: ruff
      - id: ruff-format  # Optional: format code like Black